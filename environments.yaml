name: uprobe

channels:
  - conda-forge
  - bioconda
  - defaults

dependencies:
  - python=3.8
  - pysam
  - bowtie2
  - blast
  - mmseqs2
  - pip
  - pip:
      - -r requirements.txt
