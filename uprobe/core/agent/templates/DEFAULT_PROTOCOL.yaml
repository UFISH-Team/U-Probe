name:

description: 

genome: 

targets:
  - xxx1: # chr:start-end | gene_name | name: string

encoding:
  xxx1:
    BC1: xxx
    BC2: xxx

extracts:
  target_region:
    source:   # genome | exon | CDS | UTR
    length:   # int
    overlap:   # int

probes:

# NOTE:
# - `attributes` and `post_process` can be omitted.
# - If they are missing or empty, the CLI will auto-generate defaults
#   based on DNA (source=genome) vs RNA (source=exon/CDS/UTR) mode.
attributes:


post_process:
  filters:

  sorts:

  # only dna mode
  avoid_otp:

  # only dna mode
  equal_space:

  # remove overlap
  remove_overlap:
    location_interval: # int

summary:
  report_name:  # template name  # class: dna_report or rna_report
  attributes:


